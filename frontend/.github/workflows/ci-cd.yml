name: CI/CD Pipeline

on:
  push:
    branches: [ main, master]
  pull_request:
    branches: [ main, master]

env:
  DOCKER_IMAGE: jashandeepsingh802/ci_cd
  DOCKER_TAG: ${{ github.sha }}

defaults:
  run:
    working-directory: ./

jobs:
  test:
    name: Run Tests and Lint
    runs-on: ubuntu-latest
    
    steps:
    - name: Checkout code
      uses: actions/checkout@v3
    
    - name: Set up Node.js
      uses: actions/setup-node@v3
      with:
        node-version: '18'
        cache: 'npm'
    
    - name: Install dependencies
      run: npm ci
    
    - name: Run tests
      run: npm test -- --watchAll=false
      
    - name: Run linter
      run: npx eslint . --ext .js,.jsx,.ts,.tsx
    
    - name: Build application
      run: npm run build
  
  deploy:
    name: Build and Deploy
    needs: test
    runs-on: ubuntu-latest
    if: github.ref == 'refs/heads/feature/landing_page' || github.ref == 'refs/heads/main' || github.ref == 'refs/heads/master'
    
    steps:
    - name: Checkout code
      uses: actions/checkout@v3
    
    - name: Set up Docker Buildx
      uses: docker/setup-buildx-action@v2
    
    - name: Login to Docker Hub
      uses: docker/login-action@v2
      with:
        username: ${{ secrets.DOCKERHUB_USERNAME }}
        password: ${{ secrets.DOCKERHUB_TOKEN }}
    
    - name: Build and push Docker image
      uses: docker/build-push-action@v4
      with:
        context: .
        push: true
        tags: ${{ env.DOCKER_IMAGE }}:${{ env.DOCKER_TAG }},${{ env.DOCKER_IMAGE }}:latest
        cache-from: type=gha
        cache-to: type=gha,mode=max
    
    - name: Deploy to Production
      run: |
        echo "Deployment would happen here"
        # Example deployment commands:
        # - ssh user@your-server "docker pull ${{ env.DOCKER_IMAGE }}:${{ env.DOCKER_TAG }}"
        # - ssh user@your-server "docker-compose -f /path/to/your/docker-compose.yml up -d"
